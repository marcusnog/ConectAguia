[build]
dockerfilePath = "backend/Dockerfile"
buildCommand = ""

[deploy]
startCommand = "sh -c 'npx prisma db push --skip-generate && node dist/index.js'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
